# 샘플 데이터

각 테이블의 구조를 보여주기 위한 30행 샘플이다.

- **분석·재현 용도가 아니다.** 전체 데이터는 최상단 README의 재현 방법 참조
- 원본이 합성 데이터이므로 실제 개인정보는 포함되지 않는다
- `_flag` 컬럼이 이 프로젝트의 핵심 설계이므로, 플래그가 기록된 행을
  의도적으로 10행 포함시켰다 (실제 발생률보다 높은 비율)
- 생성: `python src/make_sample.py`